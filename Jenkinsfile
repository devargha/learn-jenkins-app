
pipeline {
    agent any

    stages {
        stage('without docker') {
            steps {
                sh '''
                    echo "hello bbg from the learn-jenkins-app folder inside codespaces"
                '''
            }
        }
    }
}
