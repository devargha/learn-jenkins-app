
pipeline {
    agent any

    stages {
        stage('without docker') {
            steps {
                sh 'echo "Hello from Jenkins - written in Jenkins file inside Git Codespaces."'
            }
        }
    }
}
